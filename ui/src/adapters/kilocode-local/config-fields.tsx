// Kilo Code exposes the same adapterConfig surface as OpenCode (instructions
// file + headless permission toggle); reuse the OpenCode fields component.
export { OpenCodeLocalConfigFields as KiloCodeLocalConfigFields } from "../opencode-local/config-fields";
